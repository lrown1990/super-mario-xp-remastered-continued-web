initialY = y;
appearing = true;

mushroomType = "normal";

// Set by the block that spat it out, so a green mushroom can tell that block
// it has been taken. Empty when nobody is keeping score.
chiaveUnUp = "";

entityDirection = 1;
entityCurrentY = 0;
entitySpeed = 0.8;

depth = 101;

onGround = false;